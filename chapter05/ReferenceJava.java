package chapter05;

public class ReferenceJava {
	public static void main(String[] args)
	{
		// 기본자료형 (프리미티브)
		// - byte, short, int, long, float, double, boolean, char
		// - 변수에 실제 값이 저장
		
		// 객체자료형 (래퍼런스)
		// - class, 열거형(enum), 배열(Array), String 등
		// - 변수에 실제값이 위치한 주소가 저장
		
		// null
		// -값이 없음(x)
		// - 참조할 대상이 없는 경우!
		// - NullPointerException(에러)
		// print(name);
		//   ㄴ name 변수를 통해서 값을 가져와야 하는데 
		//      name 변수가 Null인 경우,
		//      가져올 값이 없으면 NullPointerException 발생 
	}
}
